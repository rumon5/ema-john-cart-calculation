import React from 'react';
import './Inventory.css';

const Inventory = () => {
    return (
        <div>
            <h1>This is inventory page.</h1>
        </div>
    );
};

export default Inventory;